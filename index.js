let resultado = soma(70, 11)
let rankAtual = ""
statusPlayer()
 if (soma === 11 <= 20) { console.log (rankAtual = "bronze") }
else if (soma === 21 <= 50) {console.log (rankAtual = "prata") }
else (soma === 51 <= 80); {console.log (rankAtual = "ouro") }
    


function statusPlayer(){
    console.log("O heroi tem o saldo de " + soma + " está no nível de " + (rankAtual = "") )
         
        
}


function soma(numA, numB) {
    soma = (numA - numB)
         return soma

   
}


